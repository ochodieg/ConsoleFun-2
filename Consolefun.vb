Option Strict On
Option Explicit On

Module Consolefun

    Sub Main()
        Dim userInput As String
        ''reserves variable
        '' " " <-----used to indicate a string rather than a command 
        'Console.WriteLine("please Type some text then press enter...")
        'userInput = Console.ReadLine()
        'Console.WriteLine("Thanks!! You entered: " & userInput & " Good work!!")
        'Console.ReadLine()


        'Dim firstnumber As Integer
        'Dim result As Integer
        'Console.WriteLine("please enter a number")
        'userInput = Console.ReadLine()
        'firstnumber = CInt(userInput)
        ''firstnumber = Console.ReadLine()
        'result = 5 + firstnumber

        'Console.WriteLine(result)
        'Console.ReadLine()

        Console.Beep(20000, 500)

    End Sub

End Module
